{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://assine.local/contracts/consumed/billing.subscription.canceled.v1.json",
  "title": "billing.subscription.canceled payload (v1)",
  "type": "object",
  "required": ["subscriptionId", "canceledAt"],
  "additionalProperties": true,
  "properties": {
    "subscriptionId": { "type": "string", "format": "uuid" },
    "canceledAt":     { "type": "string", "format": "date-time" },
    "reason":         { "type": ["string", "null"] }
  }
}
