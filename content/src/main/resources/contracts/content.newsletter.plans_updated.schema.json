{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://assine.com/contracts/content.newsletter.plans_updated.v1.json",
  "title": "content.newsletter.plans_updated",
  "type": "object",
  "required": ["eventId", "eventType", "schemaVersion", "aggregateType", "aggregateId", "occurredAt", "payload"],
  "properties": {
    "eventId": { "type": "string", "format": "uuid" },
    "eventType": { "const": "content.newsletter.plans_updated" },
    "schemaVersion": { "type": "integer", "const": 1 },
    "aggregateType": { "const": "Newsletter" },
    "aggregateId": { "type": "string", "format": "uuid" },
    "occurredAt": { "type": "string", "format": "date-time" },
    "payload": {
      "type": "object",
      "required": ["newsletterId", "planIds"],
      "properties": {
        "newsletterId": { "type": "string", "format": "uuid" },
        "planIds": { "type": "array", "items": { "type": "string", "format": "uuid" } }
      }
    }
  }
}
